{
  "buildCommand": "cd server && npm run build && cd .. && npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install && cd server && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.ts": {
      "runtime": "@vercel/node"
    }
  }
}
